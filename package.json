{
  "name": "mnanimat3d",
  "version": "3.2.0",
  "description": "MNAnimat3D Studio - 3D modeling, rigging, scenes and animation",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "node server.js",
    "build": "node build.js",
    "start": "node server.js",
    "deploy": "npx wrangler pages deploy ./dist --project-name=mnanimat3d",
    "deploy:assets": "npx wrangler deploy --assets=./dist"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
